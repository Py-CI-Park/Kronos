"""Model-factory layer: episode store, run registry, walk-forward, probability lane.

This package is a training/experiment orchestration layer. It never serves HTTP
and never places orders. Dashboard code consumes its artifacts read-only.
"""
